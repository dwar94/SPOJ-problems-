#include<stdio.h>
#include<iostream>
#include<vector>
using namespace std;
struct graph{
	int data;
	struct graph *next;
}*list[100001];
int main()
{
	int n,k,p;
	scanf("%d%d%d",&n,&k,&p);
	for(int i=0;i<n;i++)
	 list[0]=
	int a[n];
	for(int i=0;i<n;i++)
	  scanf("%d",&a[i]);
	for(int i=0;i<n;i++)
     for(int j=1;j<n;j++) 	
     {
     	if((a[j]-a[i])<=k||(a[i]-a[j])<=k)
     	   list[a[i]]
     } 
	
	
	return 0;
}
